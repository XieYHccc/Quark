# Quark AI Harness

This directory is the tracked AI collaboration layer for Quark engine development. It is separate from `.Quark/`, which remains local runtime/editor state.

- `skills` — stores repository skills and their agent metadata.

- `tasks` — scoped task plans with acceptance criteria and completion status.
